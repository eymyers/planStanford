<!DOCTYPE html>
<html>
  <head>
    <title>Project</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/bootstrap-theme.min.css" rel="stylesheet">
    <link href="/css/introHCI.css" rel="stylesheet">

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/js/bootstrap.min.js"></script>
    <script src="/js/introHCI.js"></script>

  </head>


  <body>

	<!-- this is a comment in HTML -->
	<div class="container">
		<div class="jumbotron">
  			<h1>{{projectName}}</h1>
  			<p>Now Select a track</p>
		</div>

    <h2>{{major}}</h2>
    {{#each tracks}}
    <div class="major">
      <a href="{{../major}}/{{this}}" class = "thumbnail">
      <button type="button" class="btn btn-default btn-lg fixedWidth">
        {{this}}
      </button>
      </a>
    </div>
    {{/each}}

    <div class="test">
      <h1>WTF</h1>
      <a class="nug"><p class="but">Ethan's Test</p></a>
    </div>

    <div class="test">
      <h1>WTF2</h1>
      <a class="nug"><p class="but">Ethan's Test 2</p></a>
    </div>

    <div class="getAll">
      <h1><a><p>Balls</p></a></h1>
    </div>

<!--     {{#each majors}}
      <h2>{{major}}</h2>
      {{#each tracks}}
      <div class="major">
        <a href="{{../major}}/{{this}}" class="thumbnail">
          <p>{{this}}</p>
        </a>
      </div>
      {{/each}}
    {{/each}} -->



    <p><a href="/">Back &raquo;</a></p>


  </body>
</html>
